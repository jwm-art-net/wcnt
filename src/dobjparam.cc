#ifndef DOBJPARAM_H
#include "../include/dobjparam.h"

#ifndef BARE_MODULES
dobjparam::dobjparam(dobjnames::DOBJ_TYPE dt, dparnames::DPAR_TYPE dpt) :
	dobjtype(dt), dpartype(dpt)
{
	// I'm a happy beef burger, but I'd like to live a bit longer please?
}

#endif

#endif
